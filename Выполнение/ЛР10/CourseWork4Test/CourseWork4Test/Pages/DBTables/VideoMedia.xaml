<Page x:Class="CourseWork4Test.Pages.DBTables.VideoMedia"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CourseWork4Test.Pages.DBTables"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1280"
      Title="VideoMedia">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontSize="28" Margin="10">Таблица видеоносителей - Video media</Label>
        <DataGrid x:Name="DataGridVM" Grid.Row="1" CanUserAddRows="False" AutoGeneratedColumns="DataGridVM_AutoGeneratedColumns" SelectionChanged="DataGridVM_Selected"></DataGrid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="25 5 0 0">
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Фильм (ID):</Label>
                    <TextBox x:Name="CinemaTB" FontSize="18" VerticalAlignment="Center" Width="615"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Поставка (ID)</Label>
                    <TextBox x:Name="VMOTB" FontSize="18" VerticalAlignment="Center" Width="598"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Тип видеоносителя (ID):</Label>
                    <TextBox x:Name="TypeTB" FontSize="18" VerticalAlignment="Center" Width="510"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Стоимость аренды в сутки:</Label>
                    <TextBox x:Name="RentPriceTB" FontSize="18" VerticalAlignment="Center" Width="485"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Прочность (0-2):</Label>
                    <TextBox x:Name="DurabilityTB" FontSize="18" VerticalAlignment="Center" Width="571"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Общая стоимость:</Label>
                    <TextBox x:Name="PriceTB" FontSize="18" VerticalAlignment="Center" Width="557"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10" >
                <Button x:Name="NewRecordBtn" FontSize="18" Margin="3" Click="NewRecordBtn_Click">Новая запись</Button>
                <Button x:Name="DeleteRecordBtn" FontSize="18" Margin="3" Click="DeleteRecordBtn_Click">Удаление</Button>
                <Button x:Name="SaveTableBtn" FontSize="18" Margin="3" Click="SaveTableBtn_Click" ToolTip="Сохраняет только выделенный элемент в таблице!" IsEnabled="False">Сохранение</Button>
                <Button x:Name="UpdateBtn" FontSize="18" Margin="3 3 3 40" Click="UpdateBtn_Click">Принудительно обновить</Button>

                <Button x:Name="GoBackBtn" Click="GoBackBtn_Click" FontSize="18">Назад</Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
