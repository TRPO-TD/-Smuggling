<Page x:Class="CourseWork4Test.Pages.DBTables.CreateDeal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
     xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:local="clr-namespace:CourseWork4Test.Pages.DBTables"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1280"
      Title="CreateDeal">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontSize="28" Margin="10">Сделки - Deal</Label>
        <DataGrid x:Name="DataGridDeal" Grid.Row="1" CanUserAddRows="False" AutoGeneratedColumns="DataGridDeal_AutoGeneratedColumns" SelectionChanged="DataGridDeal_Selected"></DataGrid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="0.7*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="25 5 0 0">
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Сотрудник (ID):</Label>
                    <TextBox x:Name="EmployeeTB" FontSize="18" VerticalAlignment="Center" Width="287"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Клиент (ID)</Label>
                    <TextBox x:Name="ClientTB" FontSize="18" VerticalAlignment="Center" Width="321"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Дата завершения сделки (гггг-мм-дд):</Label>
                    <xctk:MaskedTextBox FontSize="18" x:Name="tCompletionDateTB" Mask="0000-00-00" Width="100" Height="26"/>
                </StackPanel>
                
            </StackPanel>

            <StackPanel Grid.Column="0" Margin="10" VerticalAlignment="Center">
                <Button x:Name="ToAvaibleVMBtn" FontSize="18" Margin="3" Click="ToAvaibleVMBtn_Click">Перейти к доступным видеоносителям</Button>
                <Button x:Name="AddAvaibleVMBtn" FontSize="18" Margin="3" Click="AddAvaibleVMBtn_Click" IsEnabled="False">Добавить видеоноситель к выбранной сделке</Button>
                <Button x:Name="DeleteAvaibleVMBtn" FontSize="18" Margin="3" Click="DeleteAvaibleVMBtn_Click" IsEnabled="False">Удалить видеоноситель из выбранной сделки</Button>
                <Button x:Name="ToPenaltiesBtn" FontSize="18" Margin="3" Click="ToPenaltiesBtn_Click">Перейти к штрафам</Button>
                <!--<Button x:Name="asd" FontSize="18" Margin="3" Click="">Просмотреть штрафы по сделке</Button>
                <Button x:Name="asdss" FontSize="18" Margin="3" Click="">Добавить штраф по сделке</Button>-->

            </StackPanel>

            <StackPanel Grid.Column="2" Margin="10" >
                <Button x:Name="NewRecordBtn" FontSize="18" Margin="3" Click="NewRecordBtn_Click">Новая запись</Button>
                <Button x:Name="DeleteRecordBtn" FontSize="18" Margin="3" Click="DeleteRecordBtn_Click">Удаление</Button>
                <Button x:Name="SaveTableBtn" FontSize="18" Margin="3" Click="SaveTableBtn_Click" ToolTip="Сохраняет только выделенный элемент в таблице!" IsEnabled="False">Сохранение</Button>
                <Button x:Name="UpdateBtn" FontSize="18" Margin="3 3 3 40" Click="UpdateBtn_Click">Принудительно обновить</Button>

                <Button x:Name="GoBackBtn" Click="GoBackBtn_Click" FontSize="18">Назад</Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
