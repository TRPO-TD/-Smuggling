<Page x:Class="CourseWork4Test.Pages.DBTables.VideoMediaOrder"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CourseWork4Test.Pages.DBTables"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1280"
      Title="VideoMediaOrder">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontSize="28" Margin="10">Таблица поставок - Video media order</Label>
        <DataGrid x:Name="DataGridVMO" Grid.Row="1" CanUserAddRows="False" AutoGeneratedColumns="DataGridVMO_AutoGeneratedColumns" SelectionChanged="DataGridVMO_Selected"></DataGrid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="25 5 0 0">
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Дата заказа (гггг-мм-дд):</Label>
                    <xctk:MaskedTextBox FontSize="18" x:Name="tDateTB" Mask="0000-00-00" Width="463" Height="26"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Дата предпологаемого прибытия (гггг-мм-дд):</Label>
                    <xctk:MaskedTextBox FontSize="18" x:Name="tCompletionDateTB" Mask="0000-00-00" Width="282" Height="26"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Поставщик (ID):</Label>
                    <TextBox x:Name="ProviderTB" FontSize="18" VerticalAlignment="Center" Width="536"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Оформлявший сотрудник (ID):</Label>
                    <TextBox x:Name="EmployeeTB" FontSize="18" VerticalAlignment="Center" Width="415"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label FontSize="18" Margin=" 0 0 10 0">Поставка прибыла (1 - да, 0 - нет):</Label>
                    <TextBox x:Name="tCompletionTB" FontSize="18" VerticalAlignment="Center" Width="383"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10" >
                <Button x:Name="NewRecordBtn" FontSize="18" Margin="3" Click="NewRecordBtn_Click">Новая запись</Button>
                <Button x:Name="DeleteRecordBtn" FontSize="18" Margin="3" Click="DeleteRecordBtn_Click">Удаление</Button>
                <Button x:Name="SaveTableBtn" FontSize="18" Margin="3" Click="SaveTableBtn_Click" ToolTip="Сохраняет только выделенный элемент в таблице!" IsEnabled="False">Сохранение</Button>
                <Button x:Name="UpdateBtn" FontSize="18" Margin="3 3 3 40" Click="UpdateBtn_Click">Принудительно обновить</Button>

                <Button x:Name="GoBackBtn" Click="GoBackBtn_Click" FontSize="18">Назад</Button>
            </StackPanel>

        </Grid>
    </Grid>
</Page>
